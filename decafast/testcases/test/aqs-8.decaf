package QuickSort {
	func main() void {
		var size int;
		size = 100;
		initList(size);

		displayList(0,size);
		quickSort(0,size-1);

		print_string("After sorting:\n");
		displayList(0,size);
	}
}
