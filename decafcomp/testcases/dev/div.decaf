extern func print_int(int) void;
package Test {
    func main() int
    {
        var x int;
        if (true) {
            print_int(5 / 5);
            print_int(5 / 2);
            print_int(5 / -2);
        }
    }
}
