; ModuleID = 'C'
source_filename = "DecafComp"

@foo = global i32 42
@bar = global i1 true

declare void @print_int(i32)

declare void @print_string(ptr)

define i32 @main() {
func:
  %foo = load i32, ptr @foo, align 4
  call void @print_int(i32 %foo)
  %bar = load i1, ptr @bar, align 1
  %zexttmp = zext i1 %bar to i32
  call void @print_int(i32 %zexttmp)
  ret i32 0
}
