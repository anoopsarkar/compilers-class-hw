extern func print_int(int) void;

package foo {
    var a, b, c bool;
	func main() void {
        var flag bool;
        var size int;
        a = true;
        b = false;
        c = true;
		flag = a || b && !c;
		size = 1 >> 3 + 1 / -2 % 10 - 5 * 2 / 20 << 2;
        print_int(size);
	}
}
