extern func print_int(int) void;

package foo {
    var x bool;
	func main() void {
        var size int;
        x = !size;
		size = 1 >> 3 + 1 / -2 % 10 - 5 * 2 / 20 << 2;
        print_int(size);
	}
}
